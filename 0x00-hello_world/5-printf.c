#include <stdio.h>

/**
 * main - Print a string text using printf
 *
 * Description: This program will always print the string "with proper grammar,
 * but the outcome is a piece of art," followed by a new line
 *
 * Return: - Always 0 (Success)
 *
 */

int main(void)
{

	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", str);

	return (0);

}
